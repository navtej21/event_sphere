import 'package:flutter/material.dart';

class TicketPageView extends StatelessWidget{

  const TicketPageView({super.key});

  Widget build(BuildContext context)
  {
    return Scaffold(
      body: Center(child: Text("Ticket View"),),
    );
  }
}