import 'package:flutter/material.dart';

class EventInterestView extends StatelessWidget {
  const EventInterestView({super.key});

  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Event Interest"),
      ),
    );
  }
}
